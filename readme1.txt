Q:   What the program does?
Ans: It is used to find the index and element from where sum of elements at
     lower indices will be equal to sum of element at higher indices from the
     particular element.

Q:   How to run it?
Ans: When you will compile and run the program ,the following sample will visible to you
    
                                OUTPUT
     enter the size of array
     9
     enter array element
     -2  -5  3  7 -4  6  3  -2  -4
     
     -4 is element at index 4 from where sum of lower and higher indices element is equal